# Origins the dashboard is served from — JSON list, comma-separated inside.
CORS_ORIGINS=["http://localhost:3000"]

# Seconds a fetched ticker snapshot is reused before Yahoo Finance is asked again.
# MARKET_CACHE_TTL_SECONDS=900

# Override the committed defaults from .env.base when the port is taken.
# PORT=6100
